(ns flintstones.test)

;*****************************************************************************
;
; IMPORTANT:  Need an empty `flintstones/test.cljs` file as a "hook" so the compiler
; can find the `flintstones/test.clj` file containing the macros.
;
;*****************************************************************************
